</aside><!-- End Sidebar-->

  <main id="main" class="main">
<%= link_to 'Back', attendences_path, class: "btn btn-primary right mb-5"%>
<%= form_with(model: @attendence, url: hr_attendence_path) do |form| %>
  <% if @attendence.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@attendence.errors.count, "error") %> prohibited this attendence from being saved:</h2>

      <ul>
        <% @attendence.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :checkout_time, class: "mb-3" %>
    <%= form.datetime_field :checkout_time, class: "form-control mb-3"%>
  </div>


  <div class="actions">
    <%= form.submit class: "btn btn-primary" %>
  </div>
<% end %>

</main>