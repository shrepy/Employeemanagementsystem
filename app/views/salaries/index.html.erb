</aside><!-- End Sidebar-->

  <main id="main" class="main">

    <div class="pagetitle">
      <h1>Salary Details</h1>
      <nav>
        <ol class="breadcrumb">
          <li class="breadcrumb-item"><a href="index.html">Home</a></li>
          <li class="breadcrumb-item">Salaries</li>
          
        </ol>
      </nav>
    </div><!-- End Page Title -->

    <section class="section">
      <div class="row">
        <div class="col-lg-12">

          <div class="card">
            <div class="card-body">
              <h5 class="card-title">Simply Alliance Monthly Salary Details</h5>
              <!-- Table with stripped rows -->
              <table class="table datatable">
                <thead>
                  <tr>
                    <th scope="col">#</th>
                    <th scope="col">Employee Name</th>
                    <th scope="col">Salary</th>
                    <th scope="col">Month</th>
                    <th scope="col">Total</th>
                  </tr>
                </thead>
                <tbody>
                  <% @salaries.each do |salary| %>
                  <% emp = Employee.find(salary.employee_id) %>
                  <tr>
                    <th><%= salary.id %></th>
                    <td><%= emp.name %></td>
                    <td><%= salary.salary %></td>
                    <td><%= salary.month %></td>
                    <td><%= salary.total = salary_total %></td>
                  </tr>
                  <% end %>
                </tbody>
              </table>
              <!-- End Table with stripped rows -->

            </div>
          </div>

        </div>
      </div>
    </section>

  </main><!-- End #main -->
  